# React-ExpressJS-App-Heroku-Deployment
Example setup of a basic React and Express app.  Can be run locally or Deployed.

No need to run build first when deploying to Heroku. Heroku will run it automatically.
